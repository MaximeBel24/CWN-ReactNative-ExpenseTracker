import ModalWrapper from "@/components/ModalWrapper";
import Typo from "@/components/Typo";
import React from "react";

const TransactionModal = () => {
  return (
    <ModalWrapper>
      <Typo>Transaction Modal</Typo>
    </ModalWrapper>
  );
};

export default TransactionModal;
